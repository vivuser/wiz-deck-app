name: CI

on:
  push:
    branches:
      - develop
      - feature/**

  pull_request:
    branches:
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Simulate CI Failure
        run: |
          echo "Running CI..."
          exit 1                      















    













































    














